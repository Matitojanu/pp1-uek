import math

radius = float(5)
PI = math.pi
area = PI*radius*radius
circumference = 2*PI*radius
print(f"For a circle with a radius {radius} cm the area is {area} cm and the circumference is {circumference} cm")
